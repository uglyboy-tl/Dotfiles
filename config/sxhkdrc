# sxhkd 配置文件 - 快捷键绑定

#
# 独立于窗口管理器的快捷键
#

# 终端模拟器
super + Return # Super (Win 键) + 回车
	alacritty  # 启动 Alacritty 终端

# 程序启动器
super + r      # Super + r
	rofi -show drun # 启动 Rofi 的应用运行器 (drun 模式)

alt + Tab      # Alt + Tab
	rofi -show window # 启动 Rofi 的窗口切换器

#super + r     # Super + r (备用，当前未启用)
#	rofi -show run # 启动 Rofi 的命令运行器 (run 模式)

# 重新加载 sxhkd 配置文件
super + Escape # Super + Esc
	pkill -USR1 -x sxhkd # 向 sxhkd 进程发送 USR1 信号以重新加载配置

# 蓝牙管理脚本
super + b      # Super + b
	~/.local/bin/rofi-bluetooth # 运行自定义的 Rofi 蓝牙管理脚本

# 文件管理器
super + e      # Super + e
	alacritty --class lf -e ~/.local/bin/lf.sh # 在 Alacritty 中启动 lf 文件管理器

# 终端应用 (音乐、系统监控、新闻阅读)
super + {m, h, n} # Super + m, Super + h, Super + n
	alacritty -e {ncmpcpp, btop, newsboat} # 在 Alacritty 中分别启动 ncmpcpp, btop, newsboat

#
# 主音量控制
#
XF86AudioRaiseVolume # 音量升高键
	~/.local/bin/barify v i # 运行 barify 脚本，升高音量并显示通知

XF86AudioLowerVolume # 音量降低键
	~/.local/bin/barify v d # 运行 barify 脚本，降低音量并显示通知

XF86AudioMute        # 静音键
	~/.local/bin/barify v m # 运行 barify 脚本，切换静音状态并显示通知

#
# bspwm 快捷键
#

# 退出/重启 bspwm
super + alt + {q,r} # Super + Alt + q (退出), Super + Alt + r (重启)
	bspc {quit,wm -r} # 执行 bspc quit 或 bspc wm -r

# 关闭和杀死窗口
super + {_, shift + }w # Super + w (关闭), Super + Shift + w (杀死)
	bspc node -{c, k} # 关闭或杀死当前焦点窗口

# 在平铺和单窗口布局之间切换
super + shift + f # Super + Shift + f
    bspc desktop -l next # 切换到下一个桌面布局

#
# 窗口状态/标志
#

# 设置窗口状态
super + {t, shift + t, s, f} # Super + t (平铺), Super + Shift + t (伪平铺), Super + s (浮动), Super + f (全屏)
	bspc node -t {tiled, pseudo_tiled, floating, fullscreen} # 设置当前焦点窗口的状态

# 在浮动和平铺之间切换
super + space # Super + 空格
    bspc node -t "~"{floating,tiled} # 切换当前焦点窗口的浮动/平铺状态

# 设置节点标志
super + ctrl + {m, x, y, z} # Super + Ctrl + m (标记), Super + Ctrl + x (锁定), Super + Ctrl + y (置顶), Super + Ctrl + z (私有)
        bspc node -g {marked, locked, sticky, private} # 设置当前焦点节点的标志

#
# 焦点/交换
#

# 聚焦或发送到指定桌面
super + {_, shift + }{1-9, 0} # Super + 1-9/0 (聚焦), Super + Shift + 1-9/0 (发送)
	bspc {desktop -f, node -d} '^{1-9, 10}' # 聚焦到指定桌面或将窗口发送到指定桌面
